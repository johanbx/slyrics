<Window x:Class="slyrics.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:slyrics"
        xmlns:my="clr-namespace:slyrics"
        mc:Ignorable="d"
        Title="Slyrics by Johan Borgenstierna" 
        Height="{my:SettingBinding Height}" 
        Width="{my:SettingBinding Width}"
        Left="{my:SettingBinding Left}"
        Top="{my:SettingBinding Top}">
    <DockPanel>
        <!--DockPanel DockPanel.Dock="Top" HorizontalAlignment="Left">
            <Button Background="Yellow" Click="Button_Click">Change Lyrics</Button>
        </-->
        <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="0">
            <TextBox 
                Background="Transparent" 
                BorderThickness="0"
                IsReadOnly="True"
                Name="textArea" 
                Margin="10 0 10 10" 
                TextAlignment="Center"
                Text="Waiting for Spotify to start..."/>
        </ScrollViewer>
    </DockPanel>
</Window>
